<h3>Post a Comment</h3>

	<%= form_for [@article, @comment], remote: true do |f| %>
	<p>
		<%= f.label :content %><br/>
		<%= f.text_area :content %>
	</p>
	<p>
		<%= f.submit 'Submit' ,id:"submit-btn"%>
	</p>
	<% end %>

